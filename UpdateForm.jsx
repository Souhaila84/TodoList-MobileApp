import React from "react";

export default function UpdateForm(updateData, changeTask, updateTask, cancelUpdate){
    return(
        <div className="UpdateForm">
            <button id="cancel" className="btn btn-success" hidden>Cancel</button>
        </div>
    );
}